names:
  image: anomalica-ingester-audio
  workspace: workspace
  user: nonroot

runtime:
  features:
    - gpu
  volumes:
    - ../../staging:/mnt/staging:ro
    - ../../../anomalica-ingests:/mnt/output:rw
    - ../../shared:/mnt/shared:ro

stages:
  base:
    from: python:3.12-slim
    steps:
      - apt-get:
          install:
            - ffmpeg
            - git
      - pip:
          install:
            - torch
            - torchvision
            - whisperx
            - pyannote.audio
            - pyyaml

  development:
    from: base
    steps:
      - pip:
          install:
            - pytest

  production:
    from: base
    steps:
      - copy: workspace

commands:
  ingest:
    command: python workspace/ingest_audio.py
    description: Transcribe and diarise audio/video into Anomalica record format
    env:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: "/mnt/shared"
