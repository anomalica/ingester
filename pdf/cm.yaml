names:
  image: anomalica-ingester-pdf
  workspace: workspace
  user: nonroot

runtime:
  features: []
  # Claude Code volumes are for development only (fallback provider)
  volumes:
    - ~/.local/bin/claude:/usr/local/bin/claude:ro
    - ~/.claude:~/.claude

stages:
  base:
    from: python:3.12-slim
    steps:
      - apt-get:
          install:
            - curl
      - pip:
          install:
            - anthropic
            - pikepdf
            - pyyaml

  development:
    from: base
    steps:
      - pip:
          install:
            - pytest

  production:
    from: base
    steps:
      - copy: workspace

commands:
  ingest:
    command: python workspace/ingest_pdf.py
    description: Extract structured content from a PDF
    env:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: workspace
    mounts:
      input:
        mode: ro
      output:
        mode: rw
